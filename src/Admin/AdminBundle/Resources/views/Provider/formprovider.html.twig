{{ form_start(formAddProvider) }}
{{ form_errors(formAddProvider) }}

<div class="input-group">
	{{ form_label(formAddProvider.name) }}
	{{ form_errors(formAddProvider.name) }}
	{{ form_widget(formAddProvider.name) }}
</div>

<div>
	{{ form_widget(formAddProvider.save) }}
</div>

{{ form_end(formAddProvider) }}
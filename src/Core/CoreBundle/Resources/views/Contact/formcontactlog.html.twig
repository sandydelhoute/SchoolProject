<div class="contact">
	<div class="col-md-8">
		<h1>Nous contacter</h1>
		<p>Si vous souhaitez nous contacter, rien de plus simple ! il vous suffit de remplir les champs suivants afin que notre équipe puisse vous contacter dans les plus brefs délais.</p>
		{% for flash_message in app.session.flashBag.get('contactok') %}
			<div class="label label-success">
				{{ flash_message }}
			</div>
		{% endfor %}
		{{ form_start(form) }}

		<div class="form-group">
			{{ form_label(form.subject) }}
			{{ form_errors(form.subject) }}
			{{ form_widget(form.subject) }}
		</div>

		<div class="form-group">
			{{ form_label(form.body) }}
			{{ form_errors(form.body) }}
			{{ form_widget(form.body) }}
		</div>
		{{ form_end(form) }}

	</div>

</div>